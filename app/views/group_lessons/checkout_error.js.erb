<%
  # Parameters
  # object : an ActiveRecord object from which to read the errors
%>
<%= render template: '/shared/error', locals: { object: object } %>
$("#add_student_hidden_field").remove();
$("#new_student_hidden_field").remove();
